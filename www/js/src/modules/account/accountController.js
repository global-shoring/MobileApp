define(['require'],
    function (require) {
        'use strict';
        require([]);
        var accountController = function ($rootScope, $scope, $location, $q) {

            var self = this;
        };


        accountController.$inject = ['$rootScope', '$scope', '$location',
                                           '$q'
        ];

        return accountController;
    });